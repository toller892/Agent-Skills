# 五阶段工作流详解

## 📋 完整流程图

```
用户输入论文URL
    ↓
┌─────────────────────────────────────┐
│ 阶段1: 信息获取                      │
│ - 下载PDF                           │
│ - 提取arXiv ID                      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 阶段2: 文章生成 (黄叔风格)           │
│ - 类比密度 ≥1个/400字                │
│ - 第二人称"你" >30%                  │
│ - 三层递进解释                       │
│ - 输出: Markdown文件                 │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 阶段3: 配图生成 (纽约客风格)         │
│ - API: yunwu.ai                     │
│ - 3-4色muted配色                    │
│ - 中世纪现代美学                     │
│ - 输出: 4张PNG插画                   │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 阶段4: HTML生成 (2026设计)           │
│ - Muted暖调配色                      │
│ - Noto Serif SC + Inter字体         │
│ - Intersection Observer动画         │
│ - 输出: index.html                   │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 阶段5: PDF生成 (fpdf2)               │
│ - 原生PDF生成                        │
│ - 嵌入插画                           │
│ - 中文字体支持                       │
│ - 输出: {arxiv_id}.pdf               │
└─────────────────────────────────────┘
    ↓
完成！生成日志
```

## 🎨 设计规范

### 黄叔风格写作标准

1. **类比密度**: 每400字至少1个类比
   - ✅ "就像你在黑暗中找钥匙..."
   - ✅ "想象一下，你在处理大量数据时..."

2. **第二人称使用**: 超过30%的句子使用"你"
   - ✅ "你有没有想过..."
   - ✅ "你会发现..."

3. **三层递进解释**:
   - 第一层：是什么？
   - 第二层：为什么重要？
   - 第三层：怎么做到的？

### 纽约客插画风格

**配色方案** (3-4色muted):
- 背景: #FDFBF7 (米白)
- 主色: #7D9B76 (橄榄绿)
- 强调: #C4785A (赤陶橙)
- 辅助: #E8E4DD (浅灰)

**美学特征**:
- 中世纪现代主义
- 简洁几何形状
- 大量留白
- 无文字标注
- 概念化和隐喻性表达

### 2026前沿HTML设计

**字体组合**:
- 标题: Inter (无衬线，现代感)
- 正文: Noto Serif SC (衬线，可读性)

**交互动画**:
- Intersection Observer API
- 滚动触发淡入效果
- 平滑过渡动画

**配色**:
```css
--color-bg: #FDFBF7;        /* 背景 */
--color-primary: #7D9B76;    /* 主色 */
--color-accent: #C4785A;     /* 强调 */
--color-text: #2C2C2C;       /* 文字 */
--color-text-light: #6B6B6B; /* 次要文字 */
--color-border: #E8E4DD;     /* 边框 */
```

## 🔧 技术实现

### 阶段1: 信息获取
```python
def fetch_paper_info(self, url):
    # 下载PDF
    response = requests.get(url)
    # 提取arXiv ID
    arxiv_id = url.split('/')[-1].replace('.pdf', '')
    return paper_info
```

### 阶段2: 文章生成
```python
def generate_article(self, paper_info):
    # 应该调用Claude API生成黄叔风格文章
    # 当前版本使用模板
    markdown = generate_template()
    return paper_info
```

### 阶段3: 配图生成
```python
def generate_illustrations(self, paper_info):
    # 调用yunwu.ai API
    prompt = """Create a minimalist New Yorker style illustration
    - 3-4 muted colors
    - Mid-century modern aesthetic
    - No text"""
    image_data = call_yunwu_api(prompt)
    save_image(image_data)
```

### 阶段4: HTML生成
```python
def generate_html(self, paper_info):
    # Markdown转HTML
    html_content = markdown_to_html(markdown)
    # 应用2026设计规范
    html = apply_2026_design(html_content)
    return html
```

### 阶段5: PDF生成
```python
def generate_pdf(self, paper_info):
    pdf = FPDF()
    # 添加内容和插画
    pdf.add_page()
    pdf.image(illustration_path)
    pdf.multi_cell(text)
    pdf.output(pdf_path)
```

## 📦 目录结构

```
~/.claude/skills/paper-interpreter/
├── skill.md                # Skill定义文件
├── paper_interpreter.py    # 主程序（五阶段）
├── requirements.txt        # 依赖列表
├── README.md              # 使用说明
├── WORKFLOW.md            # 本文件
└── example_usage.py       # 使用示例
```

## 🚀 使用示例

### 基本使用
```bash
python paper_interpreter.py https://arxiv.org/pdf/2301.12345.pdf
```

### 配置API密钥
```bash
export YUNWU_API_KEY="your_key"
python paper_interpreter.py https://arxiv.org/pdf/2301.12345.pdf
```

### 在Claude Code中使用
```
你: 帮我解析这篇论文：https://arxiv.org/pdf/2301.12345.pdf
Claude: 好的，我来使用五阶段工作流为你解析...
```

## 📝 输出示例

### Markdown (黄叔风格)
```markdown
# 论文标题

你有没有想过，这篇论文到底在解决什么问题？

让我用一个类比来解释：就像你在黑暗中找钥匙...

## 三层递进解释

### 第一层：是什么？
...

### 第二层：为什么重要？
你可能会问...

### 第三层：怎么做到的？
...
```

### HTML (2026设计)
- 暖调配色
- 优雅字体
- 滚动动画
- 响应式布局

### PDF (fpdf2原生)
- 嵌入插画
- 中文支持
- 原生排版

## ⚠️ 注意事项

1. **API密钥**: 需要配置`YUNWU_API_KEY`才能生成插画
2. **中文字体**: PDF中文支持需要STHeiti字体
3. **网络连接**: 下载论文需要稳定网络
4. **处理时间**: 完整流程约需1-2分钟

## 🔮 未来改进

- [ ] 集成Claude API自动生成黄叔风格文章
- [ ] 支持更多论文源（不仅arXiv）
- [ ] 添加中文字体自动下载
- [ ] 支持自定义配色方案
- [ ] 批量处理多篇论文
- [ ] 生成PPT演示文稿
